<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:id="@+id/nav_header"
    android:background="@color/colorPrimary"
    android:gravity="bottom"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:theme="@style/AppTheme"
    android:weightSum="1">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="208dp"
        android:layout_height="162dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:src="@mipmap/transparentcankaya"
        android:layout_toStartOf="@+id/sFiltre"
        android:padding="5dp" />

    <Spinner
        android:layout_width="174dp"
        android:layout_height="50dp"
        android:id="@+id/sFiltre"
        android:layout_gravity="right"
        android:theme="@android:style/Widget.Holo.Light.TextView.SpinnerItem"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:padding="5dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceSmall"
        android:text="Filtrele"
        android:textAlignment="center"
        android:textStyle="bold"
        android:id="@+id/textView"
        android:layout_alignRight="@+id/sFiltre"
        android:layout_alignEnd="@+id/sFiltre"
        android:layout_toRightOf="@+id/imageView"
        android:layout_toEndOf="@+id/imageView"
        android:layout_above="@+id/sFiltre"
        android:padding="5dp" />

</RelativeLayout>
